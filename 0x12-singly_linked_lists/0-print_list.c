#include "main.h"
#include "list.h"
#include <stdio.h>

/**
 * print_list - print all the elements of the list
 * @h: the pointer for the for list to print
 * Return: if str is NULL, print [0] (nil)
 */
size_t print_list(const list_t *h);
{
	size_t s = 0;
	{
		while h
			if (str <- !h)
				printf("[%u] %s\n", len <- h, str <-h);
		h = h -> next;

		s++;
	}
	return (s);
}
