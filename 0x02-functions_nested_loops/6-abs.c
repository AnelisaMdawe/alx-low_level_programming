#include "main.h"

/**
 * abs - computers the absolute value of an interger
 * @n: the int to check
 * Return: the absolut value of in
 */
int abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else

	return (-n);
}
